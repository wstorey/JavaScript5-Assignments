import { DecorationDirective } from './decoration.directive';

describe('DecorationDirective', () => {
  it('should create an instance', () => {
    const directive = new DecorationDirective();
    expect(directive).toBeTruthy();
  });
});
